
# AGENTS.md - digirepxyz

This file is the operating guide for coding agents working in this repository (`digirepxyz`). This project is created/managed using `bun`. Built with Expo v57 (React Native).  Read documentation (`docs/`), especially development documentation (`docs/development/`).

## STRICT RULES

These rules are **non-negotiable** and apply to every task, every time. Violations are not acceptable regardless of task size or urgency. 

1. **Hand-by-Hand Development Workflow.**
Each operation is executed collaboratively and incrementally with the user. Explain why, how and when did you implement every function, type, class, etc. When you get uncertain about a decision, ASK ME FOR CONFIRMATION! Do not do ANYTHING not explicitly requested. When encountering ANY error, missing file, test mismatch, or design decision: STOP and ASK FOR CONFIRMATION before taking action.

2. **Never Attempt Silent Workarounds.** 
Never attempt silent workarounds (e.g. refactoring code, rewriting tests, or changing behavior). When a package or dependency is missing, ASK FOR CONFIRMATION and BRAINSTORM with the user. Do not install by your own. Ask me explicitly first, when you think you need to install a package. Ask me explicitly first, when you think you need to install a package. When you get stuck, ASK FOR HELP! Especially important for project package management. 

3. **Adhere Strictly to Expo SDK v57.**
Always consult and follow the Expo SDK v57 documentation (React 19, React Native 0.86, Expo Router v57).

4. **Develop Strictly by KISS Principles.**
Keep implementations concise, simple, and specific. Do not lean on generic structures unless requested. Do not use advanced language features unless requested or strictly required.
